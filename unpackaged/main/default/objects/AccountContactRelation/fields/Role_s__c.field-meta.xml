<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Role_s__c</fullName>
    <externalId>false</externalId>
    <formula>IF(INCLUDES(Roles, "Current Employee"), "Current Employee;",
IF(INCLUDES(Roles, "Previous Employee"), "Previous Employee;",
IF(INCLUDES(Roles, "Client"), "Client;",
IF(INCLUDES(Roles, "Other"), "Other;" , ""))))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Role(s)</label>
    <required>false</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
